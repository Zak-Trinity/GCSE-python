total = 0
temp_num = 0
add = None
for i in range(5):
    temp_num = int(input("Enter a number: "))
    add = input("Do you want the number you entered to be added to the total? (press y or n): ").strip().upper()
    if add == "Y":
        total += temp_num
    
print(total)

